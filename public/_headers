# Force revalidation of HTML so browser always gets latest asset URLs (avoids stale cache → provisional headers / broken load)
/index.html
  Cache-Control: no-cache, no-store, must-revalidate

# Cache hashed assets; they never change for the same URL
/assets/*
  Cache-Control: public, max-age=31536000, immutable
