<textarea
  class="form-field__input {{className}}"
  id="{{id}}"
  name="{{name}}"
  rows="{{rows}}"
  {{#if placeholder}}placeholder="{{placeholder}}"{{/if}}
  {{#if ariaLabel}}aria-label="{{ariaLabel}}"{{/if}}
  {{#if required}}required{{/if}}
  ref="{{id}}"
>{{value}}</textarea>
